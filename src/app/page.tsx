import { nunito } from "./layout";

export default function Home() {
  return (
   <div>
    <span className={`${nunito.className} font-bold text-2xl bg-gradient-to-tr from-25% from-blue-500 to-80% to-blue-400 text-ellipsis text-transparent bg-clip-text`}>NOLO</span>
    <div>hihi</div>
   </div>
  );
}
